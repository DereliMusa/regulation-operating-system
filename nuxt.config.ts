// Nuxt configuration.
// See https://nuxt.com/docs/api/configuration/nuxt-config
// Modules (UI, fonts, eslint, auth) are added in the chore(deps) step.
export default defineNuxtConfig({
  compatibilityDate: '2026-07-01',
  devtools: { enabled: true },
})
